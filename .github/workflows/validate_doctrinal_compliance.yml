name: Validate Doctrinal Compliance\n\non: [push, pull_request]\n\njobs:\n  validate:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout repository\n        uses: actions/checkout@v2\n      - name: Validate Doctrinal Compliance\n        run: |\n          # Replace `your-grok-api` with the actual Grok API you intend to use.\n          curl -X POST \n          -H 'Content-Type: application/json' \n          -d '{ "source": "${{ github.workspace }}" }' \n          your-grok-api/validate \n          --fail-fast\n