# High Frequency mode — runs every hour for 24/7 surveillance
name: 24/7 Hunter Surveillance

on:
  schedule:
    - cron: '0 * * * *'  # Runs every hour
  workflow_dispatch:

jobs:
  hunter-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Hunter Scan
        run: echo "24/7 Hunter Surveillance scan running — Always Watching."
